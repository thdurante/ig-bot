<h1><%= @client.user.username %>'s recent media</h1>
<% @client.user_recent_media.each do |media_item| %>
    <div>
      <%= image_tag media_item.images.standard_resolution.url %> <br/>
      <p>LikesCount = <%= media_item.likes[:count] %></p>
    </div>
    <br/>
<% end %>